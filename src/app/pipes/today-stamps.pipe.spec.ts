import { TodayStampsPipe } from './today-stamps.pipe';

describe('TodayStampsPipe', () => {
  it('create an instance', () => {
    const pipe = new TodayStampsPipe();
    expect(pipe).toBeTruthy();
  });
});
